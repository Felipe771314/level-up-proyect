import { screen } from '@testing-library/react'
import { beforEach, describe,expect,it } from 'vitest'


import  { {{name}} } from './{{name}}'
import type { Props } from './{{name}}.types'

describe('{{name}}', () => {
   let defaultProps: Props

   beforEach(()=> {
      defaultProps = {

      }
   })
   it( "Should render component", async() =>{
   })
})
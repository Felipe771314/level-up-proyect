import * as React from 'react'
import * as Styled from './img.styled'
import type { Props } from './img.types'


export const  img:React.FC<Props> = (props: Props) => {
    return <Styled.container />
    }
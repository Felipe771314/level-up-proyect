import { img } from './img';
import type { Props } from './img.types';

describe(`img`, () => {
    let defaultProps: Props;

    beforeEach(() => {
        defaultProps = {
            // Define tus defaultProps aquí si es necesario
        };
    });

    it("Should render component", async () => {
        // Escribe tus pruebas aquí
    });
});